#!/usr/bin/env groovy

node {
    tools {
        maven 'maven'
    }
    
    auroraMaven {
        buildAuroraMaven 'clean install'
        buildAuroraMaven 'clean install'
        buildAuroraMaven 'clean install'
    }
}