* Make sure the junit jupiter dependencies are up to date
* Check if many dependencies can be replaced by one
* Is junit-platform-launcher necessary?
* What does using 
```
  <dependency>
      <groupId>org.junit.jupiter</groupId>
      <artifactId>junit-jupiter</artifactId>
      <version>${junit.jupiter.version}</version>
      <scope>test</scope>
  </dependency>
```
 in isolation do?
 * 